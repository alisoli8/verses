# Environment Variables for Verses App
# Copy this file to .env.local for local development
# For production (Vercel), add these in the Vercel Dashboard > Settings > Environment Variables

# ============================================
# CLIENT-SIDE VARIABLES (exposed to browser)
# These are safe to expose - protected by RLS or rate limits
# ============================================

# Required: Supabase Configuration (anon key is safe - RLS protects data)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Optional: Cloudinary for Image Uploads (upload preset is safe)
VITE_CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
VITE_CLOUDINARY_UPLOAD_PRESET=verses_uploads

# ============================================
# SERVER-SIDE VARIABLES (never exposed to browser)
# These are proxied through /api/* routes
# ============================================

# Required: Gemini API Key for AI features
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Serper API for Google Image Search (2500 free searches)
SERPER_API_KEY=your_serper_api_key_here

# Optional: Pollinations API for AI Image Generation
POLLINATIONS_API_KEY=your_pollinations_api_key_here

# Optional: Openverse API for Creative Commons Image Search
OPENVERSE_CLIENT_ID=your_openverse_client_id_here
OPENVERSE_CLIENT_SECRET=your_openverse_client_secret_here
